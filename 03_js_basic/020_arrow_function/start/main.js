//関数宣言
function fn(number) {
	return number * 2;
}

//関数式
const fn = function (number) {
	return number * 2;
};

//関数式のアロー関数
const fn = (number) => {
	return number * 2;
};

//関数式のアロー関数の省略記法
const fn = (number) => number * 2;
